extends ./header.jade

block content
  .container.starter-template
    h3 At home, you have:
    each ingredient in ingredients
      p= ingredient
    h3 You don't have:
    each ingredient in missingIngredients
      p= ingredient
    h3 I suggest:
    each cocktail in allCocktails
      p= cocktail
